---
kind: PersistentVolume
apiVersion: v1
metadata:
  labels:
    type: local-storage
  name: jenkins-pv
  namespace: jenkins
spec:
  capacity:
    storage: 2500Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  local:
    path: /datadrive/jenkins
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: role
          operator: In
          values:
          - "worker"
  persistentVolumeReclaimPolicy: Retain
