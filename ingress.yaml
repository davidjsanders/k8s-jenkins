apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: jenkins-ingress
  namespace: jenkins
spec:
  rules:
  - host: jenkins.40.87.66.206.xip.io
    http:
      paths:
      - path: /
        backend:
          serviceName: jenkins-service
          servicePort: jenkins-ui
